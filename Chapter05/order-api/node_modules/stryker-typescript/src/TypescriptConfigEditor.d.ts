import * as ts from 'typescript';
import { ConfigEditor, Config } from 'stryker-api/config';
export default class TypescriptConfigEditor implements ConfigEditor {
    private readonly log;
    edit(strykerConfig: Config, host?: ts.ParseConfigHost): void;
    private loadTSConfig;
    private overrideOptions;
    private readTypescriptConfig;
}
