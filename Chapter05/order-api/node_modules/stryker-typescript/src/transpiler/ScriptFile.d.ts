export default class ScriptFile {
    name: string;
    content: string;
    version: number;
    constructor(name: string, content: string, version?: number);
    replace(newContent: string): void;
}
