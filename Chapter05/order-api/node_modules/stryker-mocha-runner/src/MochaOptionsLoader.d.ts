import { StrykerOptions } from 'stryker-api/core';
import MochaRunnerOptions from './MochaRunnerOptions';
export default class MochaOptionsLoader {
    private readonly log;
    private readonly DEFAULT_MOCHA_OPTS;
    load(config: StrykerOptions): MochaRunnerOptions;
    private parseOptsFile;
    private parseNextInt;
    private parseNextString;
}
